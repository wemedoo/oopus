@{
    ViewBag.Title = "ReloadForms";
}


@*@await Html.PartialAsync("~/Views/Form/FormFilter.cshtml")*@
<div class="table-responsive centered-table filter-border">
    <table class="table table-striped table-sm">
        <thead class="color-header">
            <tr class="table-header-row">
                <th>@sReportsV2.Cache.Resources.TextLanguage.Title.ToUpper()</th>
                <th>@sReportsV2.Cache.Resources.TextLanguage.NumberOfDocuments.ToUpper()</th>
                <th></th>
            </tr>
        </thead>
        <tbody>

            @foreach (sReportsV2.DTOs.FormDistribution.DataOut.FormDistributionTableDataOut form in Model.Data)
            {
                <tr class="table-content-row">
                    <td onclick="clickedRow(event, true,'@form.Id')" id="row-@form.Id">@form.Title</td>                   
                    <td>
                        <fieldset class="form-element form-input">
                            <input type="number" id="num_of_documents_@form.Id" min="0" required class="form-element-field" placeholder="@sReportsV2.Cache.Resources.TextLanguage.SetNumberOfDocumentsToGenerate" />
                            <div class="form-element-bar"></div>
                         </fieldset>
                    </td>     
                    <td>
                        <button class="btn btn-create" type="button" onclick="generateDocuments('@form.Id')">@sReportsV2.Cache.Resources.TextLanguage.GenerateDocuments</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <div>
        <div class="pager-container">
            @await Html.PartialAsync("Pagination")
        </div>
    </div>
</div>

